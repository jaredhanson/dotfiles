# ~ / .dotfiles / flyctl

[`flyctl`](https://fly.io/docs/flyctl/) allows managing and deploying
applications on [Fly.io](https://fly.io/).

## Install

```sh
brew install flyctl
```

## Usage

To log in, execute:

```sh
$ flyctl auth login
```
